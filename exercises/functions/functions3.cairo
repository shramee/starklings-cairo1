// functions3.cairo
// Execute `starklings hint functions3` or use the `hint` watch subcommand for a hint.

// I AM NOT DONE
use debug::PrintTrait;

fn main() {
    call_me();
}

fn call_me(num: u64) {
    num.print();
}
