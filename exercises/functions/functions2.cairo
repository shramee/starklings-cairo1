// functions2.cairo
// Execute `starklings hint functions2` or use the `hint` watch subcommand for a hint.

// I AM NOT DONE
use debug::PrintTrait;

fn main() {
    call_me(3);
}

fn call_me(num:) {
    num.print();
}
