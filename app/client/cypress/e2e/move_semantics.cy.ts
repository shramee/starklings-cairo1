describe("move_semantics", () => {
  it("move_semantics1", () => {
    cy.testExercise("move_semantics1");
  });

  it("move_semantics2", () => {
    cy.testExercise("move_semantics2");
  });

  it("move_semantics3", () => {
    cy.testExercise("move_semantics3");
  });

  it("move_semantics4", () => {
    cy.testExercise("move_semantics4");
  });

  it("move_semantics5", () => {
    cy.testExercise("move_semantics5");
  });

  it("move_semantics6", () => {
    cy.testExercise("move_semantics6");
  });
});
