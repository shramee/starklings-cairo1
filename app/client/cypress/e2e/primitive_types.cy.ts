describe("primitive_types", () => {
  it("primitive_types1", () => {
    cy.testExercise("primitive_types1");
  });

  it("primitive_types2", () => {
    cy.testExercise("primitive_types2");
  });

  it("primitive_types3", () => {
    cy.testExercise("primitive_types3");
  });

  it("primitive_types4", () => {
    cy.testExercise("primitive_types4");
  });

});
